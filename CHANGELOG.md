# История изменений (Changelog)

Все значимые изменения в проекте будут документированы в этом файле.

Формат основан на [Keep a Changelog](https://keepachangelog.com/ru/1.0.0/).

## [Unreleased]

## [2026-01-05 v1.3.1] - Переключатель Text/Stage в результатах поиска Custom режима

### Добавлено
- **Переключатель между текстом и стенограммой в результатах векторного поиска:**
  - Добавлены кнопки Text/Stage для переключения между полями `text` и `stage` в результатах Custom API
  - Кнопки отображаются inline справа от имени архива (экономия вертикального пространства)
  - Переключатель появляется автоматически только если поле `stage` не пустое
  - Активная кнопка визуально выделена (синий фон и белый текст)
  - Состояние переключателя сохраняется в localStorage и восстанавливается после перезагрузки
  - Состояние переключателя сохраняется при любых ре-рендерах сообщений

### Улучшено
- **Автоматическое извлечение даты из имени архива:**
  - Если поле `meeting_date` пустое, дата извлекается из имени файла архива
  - Поддерживаются форматы: YYYY-MM-DD, YYYY.MM.DD, DD.MM.YYYY, DD-MM-YYYY, YYYYMMDD и другие
  - Дата отображается в формате DD.MM.YYYY
- **Обработка переносов строк:**
  - Строки `\n` в полях `text` и `stage` автоматически преобразуются в реальные переносы строк
  - CSS свойство `white-space: pre-wrap` сохраняет форматирование текста

### Исправлено
- Обратная совместимость с сохраненными сообщениями в localStorage (старый формат onclick handlers)
- Корректная регенерация HTML результатов поиска с сохранением состояния панелей при ре-рендерах

## [2026-01-05 v1.2.6] - Переработка UI кнопок режимов и исправления загрузки файлов

### Улучшено
- **Переработан интерфейс кнопок режимов работы (SQL/Custom):**
  - Кнопки режимов перенесены из отдельной вертикальной панели в sidebar (горизонтально, справа от кнопки "New chat")
  - Кнопки теперь компактные квадратные 32×32px с иконками (без текста)
  - Активная кнопка имеет полупрозрачный синий фон и синюю рамку для визуального отличия
  - Кнопки режимов автоматически скрываются если доступен только 1 режим (нет выбора)
  - Удалена отдельная вертикальная панель `.mode-panel`
  - Исправлен layout приложения с 3 колонок на 2 (sidebar + main area)
  - Main area теперь занимает всю доступную ширину

### Исправлено
- **Загрузка и разархивирование .7z файлов:**
  - Исправлено извлечение правильного имени файла из заголовка `Content-Disposition` HTTP-ответа
  - Теперь разархивированные файлы сохраняются с правильным расширением (например, `file.txt` вместо `file.7z`)
  - Браузер корректно отображает имя файла в списке загрузок после разархивирования
  - Добавлено логирование для отладки процесса загрузки и разархивирования

### Изменено
- Добавлены текстовые метки (labels) для всех режимов работы во всех схемах БД в конфигурации (config.js)

## [2025-12-31 v1.2.5] - Оптимизация производительности рендеринга чата

### Улучшено
- **Значительное ускорение рендеринга сообщений чата:**
  - Использование DocumentFragment для пакетной вставки всех сообщений (вместо последовательного добавления)
  - Ленивый рендеринг свернутых сообщений - тяжелый контент (markdown, SQL, таблицы) не создается для свернутых сообщений
  - Асинхронная подсветка синтаксиса SQL через requestIdleCallback - выполняется порциями во время простоя браузера
  - Уменьшено количество reflow и repaint операций
- **Результат:** Рендеринг чата с 50+ сообщениями стал в несколько раз быстрее, интерфейс остается отзывчивым даже при большом количестве сообщений

## [2025-12-31 v1.2.4] - Улучшение визуальной индикации процессов

### Улучшено
- Разные цвета фона для индикаторов процессов:
  - "Generating response…" - синий фон (более заметный в тёмной теме)
  - "Receiving data…" - зелёный фон
- Улучшена видимость индикаторов на тёмном фоне

## [2025-12-31 v1.2.3] - Улучшение отображения графиков

### Исправлено
- Автоматическое масштабирование оси Y для линейных и столбчатых графиков
- График больше не "прижимается" к верху при значениях в узком диапазоне (например, 4000-6000)
- Добавлен 5% отступ сверху и снизу для лучшей читаемости графиков

## [2025-12-30 v1.2.1] - Полная система аутентификации и улучшения UI

### Добавлено
- **Система аутентификации с Flask-Login**
  - SQLite база данных для хранения пользователей (auth.db)
  - Модели: User, UserSchemaAccess, ActivityLog
  - Хеширование паролей с bcrypt (cost factor 12)
  - Функция "Запомнить меня" с TTL 30 дней
  - Восстановление пароля через токены (TTL 1 час)
  - Страница входа с модальным окном восстановления пароля

- **Разграничение доступа к схемам БД**
  - 7 схем: СОЗД, Гаазе, Семья, Игра, ГЕО-ЭКО, ЕВГЕНИЯ, Наш дом Израиль
  - Пользователи видят только доступные им схемы
  - Декораторы: `@login_required`, `@admin_required`, `@schema_access_required`
  - API endpoint `/api/schemas` для получения доступных схем

- **Админ панель**
  - Модальное окно с вкладками "Пользователи" и "История активности"
  - CRUD операции для пользователей (создание, редактирование, удаление)
  - Управление доступом к схемам (7 чекбоксов)
  - Сброс пароля пользователя
  - Таблица истории активности с фильтром по пользователю
  - Автообновление таблицы истории при выборе пользователя
  - Компактное размещение кнопок действий в одной строке

- **Новые файлы**
  - `models.py` - модели SQLite (User, UserSchemaAccess, ActivityLog)
  - `auth_service.py` - сервисы управления пользователями
  - `auth_middleware.py` - декораторы защиты endpoints
  - `init_db.py` - скрипт инициализации БД
  - `bump_version.py` - утилита версионирования
  - `templates/login.html` - страница входа
  - `static/js/login.js` - логика входа
  - `static/js/modules/auth.js` - модуль аутентификации
  - `static/js/modules/admin.js` - модуль админ панели

### Улучшено
- **UI админ панели**
  - Кнопка "Добавить пользователя" перемещена в панель вкладок
  - Форма редактирования пользователя сделана компактной по вертикали
  - Добавлен скроллинг для поля "Доступ к схемам БД"
  - Добавлен скроллинг для всей формы редактирования
  - Исправлен курсор (pointer только на кнопках)
  - Удалены borders из колонок "Статус" и "Роль"
  - Убран фон у badges (только цвет текста)

- **Основной UI**
  - Адаптивная grid-сетка с гибкой шириной (minmax)
  - Основная область контента расширена до края браузера
  - Sidebar с минимальной шириной 250px без скрытия
  - Горизонтальный скроллинг при необходимости
  - Кнопка инструментов (⚙️) перемещена перед "New chat"
  - Добавлена CSS-переменная `--hover` для обеих тем
  - Фон меню инструментов меняется при наведении

### Исправлено
- **Критический баг с дубликатом ID**
  - Поле email не заполнялось при редактировании пользователя
  - Причина: два элемента с `id="userEmail"` (DIV и INPUT)
  - Решение: переименован DIV в `id="userEmailDisplay"`

- **Баг мерцания формы входа**
  - Форма login постоянно обновлялась (дергалась)
  - Причина: автоматическая проверка авторизации в login.js
  - Решение: удалена функция `checkAuthentication()`

- **Баг цветов схем в светлой теме**
  - Выпадающий список схем показывал белый текст на белом фоне
  - Решение: добавлены явные стили для обеих тем

- **Баг пустых имен схем**
  - В dropdown выводились пустые строки
  - Причина: API возвращал только values без labels
  - Решение: API теперь возвращает `{value, label}` объекты

### Технические изменения
- `sozd_chat.py`: Интеграция Flask-Login, все auth/admin endpoints
- `config.py`: Настройки аутентификации (SECRET_KEY, DATABASE_PATH, SESSION_COOKIE_*)
- `requirements.txt`: Добавлены bcrypt, flask-login, python-dotenv
- `templates/index.html`: User info, кнопка выхода, админ панель
- `static/js/app.js`: Проверка авторизации, фильтрация схем
- `static/css/styles.css`: Стили для login, админ панели, --hover переменная

### API Endpoints
- `POST /api/auth/login` - вход
- `POST /api/auth/logout` - выход
- `GET /api/auth/me` - текущий пользователь
- `GET /api/schemas` - доступные схемы
- `GET /api/admin/users` - список пользователей (admin)
- `POST /api/admin/users` - создание (admin)
- `PUT /api/admin/users/<id>` - обновление (admin)
- `DELETE /api/admin/users/<id>` - удаление (admin)
- `POST /api/admin/users/<id>/reset-password` - сброс пароля (admin)
- `GET /api/admin/activity` - история (admin)

## [2025-12-29 v1.0.5.5 18:00] - Автоматическая очистка кэша при ошибках

### Добавлено
- **Автоматическая очистка кэша запроса при ошибках**
  - При любой ошибке в запросах к URL_rest или URL автоматически вызывается очистка кэша
  - Отправляется POST на `v1/cache/clear/query` с текстом запроса и схемой БД
  - Предотвращает кэширование ошибочных ответов
  - Логирование успешной очистки и предупреждения при неудаче

### Исправлено
- **URL_rest в конфигурации**
  - Исправлен с локального `http://127.0.0.1:5050/` на продакшн `https://159.223.0.234:5051/`
  - Добавлена автоматическая проверка URL_rest в скрипте deploy.sh
  - Deploy прерывается если URL_rest имеет неверное значение

### Технические изменения
- `static/js/modules/render.js`: Добавлена очистка кэша в обработчики ошибок (строки 1193-1199, 1107-1112)
- `static/js/modules/config.js`: Исправлен URL_rest на продакшн значение
- `deploy.sh`: Добавлена проверка URL_rest перед деплоем

## [2025-12-29 v1.0.5.4 15:30] - Улучшения UI и новый функционал

### Добавлено
- **Кнопка очистки кэша запроса**
  - Отображается слева от поля ввода Message
  - Видна только при непустом поле ввода
  - Автоматически скрывается после отправки сообщения
  - Работает при вводе текста, вставке (Ctrl+V) и навигации по истории (↑↓)
  - Отправляет POST запрос на `v1/cache/clear/query` с текстом запроса и схемой БД
  - Запрашивает подтверждение перед очисткой кэша
  - Показывает результат операции в alert

- **Вывод URL в информации о версии**
  - Добавлен `URL_api` (https://159.223.0.234:5001/)
  - Добавлен `URL_rest` (https://159.223.0.234:5051/)
  - Отображаются при нажатии на "Версия API gemini" в меню инструментов

- **Индикатор прогресса загрузки**
  - "Generating response…" - при запросе к URL_rest (генерация SQL)
  - "Receiving data…" - при запросе к URL (выполнение SQL)
  - Автоматическое изменение текста в зависимости от этапа

### Изменено
- **Кнопки режимов работы**
  - Перемещены из топбара в отдельную вертикальную панель
  - Расположены между sidebar и main областью
  - Размещены вертикально (одна под другой)
  - Выровнены по верхнему краю панели
  - Фиксированный размер 30×30px для стабильности layout
  - Добавлен `flex-shrink: 0` для предотвращения сжатия

- **Layout приложения**
  - Grid layout изменён с `320px 1fr` на `320px auto 1fr`
  - Добавлена третья колонка для панели режимов
  - Минимальная ширина панели режимов: 50px

### Исправлено
- **Цвета полей в тёмной теме**
  - Исправлено отображение выпадающего списка схем БД
  - Исправлено отображение поля поиска чатов
  - Заменён несуществующий `var(--bg-primary)` на `var(--panel)`
  - Добавлены стили для `option` элементов
  - Добавлены стили для placeholder текста

- **Высота топбара**
  - Исправлено изменение высоты при переключении режимов
  - Добавлен `min-height: 58px` для `.topbar`
  - Кнопки режимов теперь имеют фиксированные размеры вместо минимальных

### Технические изменения
- `static/js/modules/api.js`: Добавлена функция `clearQueryCache()`
- `static/js/modules/ui.js`: Добавлена функция `setOverlayText()`
- `static/js/app.js`: Логика показа/скрытия кнопки очистки кэша, импорт config
- `static/css/styles.css`: Стили для вертикальной панели, кнопки очистки кэша, исправления темной темы
- `templates/index.html`: Добавлена панель `.mode-panel`, кнопка очистки кэша

## [2025-12-28 v1.0.5.2 18:35] - Исправлена блокировка запросов после abort

### Исправлено
- **Критическая ошибка блокировки запросов**
  - После прерывания запроса (abort) флаги состояния не сбрасывались
  - Все последующие запросы игнорировались из-за `isGenerating === true`
  - Кнопка Send оставалась в состоянии "Stop"
  - Overlay (затемнение) не убиралось
  - Смена схемы БД не работала

### Изменено
- `static/js/app.js`: Добавлен сброс флагов после вызова `abort()` (строки 370-375)
  - Сброс `isGenerating` через `setIsGenerating(false)`
  - Сброс `currentAbortController` через `setCurrentAbortController(null)`
  - Сброс состояния кнопки через `setGenerating(sendBtn, false)`
  - Сброс overlay через `setOverlay(genOverlay, false)`
  - Разблокировка кнопки голосового ввода

### Добавлено
- `.dockerignore`: Создан файл для исключения ненужных файлов из Docker образа
  - Исключены `.venv`, `.git`, `.idea`, `.claude`
  - Исключены `__pycache__`, `*.pyc`, временные файлы
  - Исключены `gcs_credentials.json` (передаются через env vars)
  - Исключены скрипты деплоя, документация, DDL схемы

## [2025-12-27 v1.0.5.1 13:46] - Добавлена очистка кэша схемы и скачивание Excel

### Добавлено
- **Меню инструментов**
  - Новый пункт "Очистить кэш схемы" (🧹)
  - Отправляет DELETE запрос на `v1/cache/clear/schema/{db_schema}`
  - Показывает подтверждение с названием схемы перед очисткой

### Изменено
- **Кнопка экспорта таблиц**
  - Изменена с "Copy CSV" на "Excel"
  - Вместо копирования в буфер обмена теперь скачивает CSV файл
  - Имя файла: `{Название_чата}_{timestamp}.csv`
  - Добавлен индикатор процесса создания файла "⏳ Обновление…"
  - CSV файл с BOM для корректного отображения кириллицы в Excel

### Улучшено
- **Подсказки (title) для кнопок**
  - Clear - "Очистить все сообщения текущего чата"
  - Export - "Экспортировать все чаты в JSON файл"
  - Copy SQL - "Скопировать SQL запрос в буфер обмена"
  - Show/Hide SQL - "Показать/Скрыть SQL запрос"
  - Excel - "Скачать таблицу в формате Excel (CSV)"

### Технические детали
- `static/js/modules/api.js`: Добавлена функция `clearSchemaCache(schema)`
- `templates/index.html`: Добавлен пункт меню и title для кнопок
- `static/js/app.js`: Добавлен обработчик для очистки кэша схемы
- `static/js/modules/render.js`: Изменён обработчик кнопки Excel с индикацией процесса

## [2025-12-26 v1.0.5 19:44] - Новая версия

### Изменено
- Переход на новую версию 1.0.5

## [2025-12-26 v1.0.4.9 12:00] - Добавлено меню инструментов

### Добавлено
- **Меню инструментов**
  - Кнопка инструментов (⚙️) рядом с кнопкой "New chat"
  - Выпадающее меню с двумя пунктами:
    - Версия API - получение версии через GET v1/version
    - Очистка кэша - очистка кэша через DELETE v1/cache/clear
  - Меню автоматически закрывается при клике вне его
  - Непрозрачный фон меню (var(--panel))
  - Z-index 9999 для перекрытия всех элементов под меню

### Технические детали
- `templates/index.html`: Добавлена структура меню инструментов
- `static/css/styles.css`: Стили для кнопки и выпадающего меню
- `static/js/modules/api.js`: Функции getApiVersion() и clearApiCache()
- `static/js/app.js`: Обработчики событий для меню

## [2025-12-24 v1.0.4.8 17:36] - Увеличена минимальная ширина URL колонок до 200px

### Исправлено
- URL колонки теперь имеют минимальную ширину 200px
- URL больше не разбивается на множество строк в таблице
- Применяется к заголовкам (th) и ячейкам (td) URL колонок

### Технические детали
- `static/js/modules/render.js`: Создание `urlColumns` Set для идентификации URL колонок
- Установка `min-width: 200px` для th и td элементов URL колонок
- Обнаружение URL через regex анализ первых 5 строк данных

## [2025-12-24 v1.0.4.7 17:23] - Выбор схемы БД и оптимизация отображения таблиц

### Добавлено
- **Выбор схемы базы данных**
  - Dropdown-селектор схемы БД в верхней панели (6 схем: СОЗД, Гаазе, Семья, Игра, ГЕО, ЕВГЕНИЯ)
  - Каждый чат привязан к конкретной схеме БД
  - Список чатов фильтруется по выбранной схеме
  - Автоматическое создание нового чата при переключении на схему без чатов
  - Миграция старых чатов: автоматическое присвоение первой схемы (sozd)
  - Схема сохраняется в localStorage (ключ: `chatui_db_schema`)
  - Параметр `db_schema` передаётся на backend в теле запроса

- **Статистика по схеме**
  - Кнопка U показывает статистику только для текущей схемы
  - Формат: "Название схемы • N чатов • Размер"
  - Динамическое обновление при переключении схемы

- **Прокручиваемый список чатов**
  - Список чатов в боковой панели теперь прокручивается при большом количестве
  - Кастомные стили scrollbar с поддержкой светлой и тёмной темы
  - Сохранение корректной работы flex-контейнера

### Изменено
- **Оптимизация таблиц**
  - Максимальное количество колонок увеличено с 10 до 20
  - Максимальная высота таблицы увеличена с 360px до 500px
  - Максимальная ширина ячейки увеличена с 400px до 600px
  - Текст в ячейках с переносом строк (white-space: normal)

- **Многострочный текст в таблицах**
  - Ячейки с символами `\n` автоматически отображаются как `<textarea>`
  - Автоматический расчёт высоты textarea по содержимому (scrollHeight + 4px)
  - Стили textarea: без border, без resize, прозрачный фон, readonly
  - Предотвращение появления scrollbar (overflow-y: hidden)

- **Оптимизация ширины колонок**
  - Числовые колонки: автоматическое определение (анализ первых 5 строк)
  - Числовые колонки: max-width 120px, выравнивание вправо, без переноса
  - Текстовые колонки: динамическая ширина на основе анализа содержимого
  - Анализ максимальной длины текста в первых 5 строках
  - Пропорциональное распределение ширины (10%-60% диапазон)
  - URL колонки: минимальная эффективная длина 100 символов

- **Улучшения кода**
  - Параметры не отображаются, если объект params пустой
  - Удалены все выполненные TODO комментарии из `sozd_chat.py`

### Технические детали
- **Файлы**:
  - `static/js/modules/config.js`: Добавлены DB_SCHEMAS, DB_SCHEMA_KEY, MAX_TABLE_COLS=20
  - `static/js/modules/state.js`: Добавлена переменная dbSchema, функции setDbSchema(), миграция чатов
  - `static/js/modules/api.js`: Параметр db_schema в теле запроса
  - `static/js/app.js`: Инициализация селектора схемы и обработчик изменения
  - `static/js/modules/render.js`: Фильтрация чатов, textarea для многострочных ячеек, анализ ширины колонок
  - `static/js/modules/actions.js`: Параметр schema в createChat(), deleteChat(), newChat()
  - `static/js/modules/stats.js`: Фильтрация по схеме, отображение названия схемы
  - `templates/index.html`: Добавлен `<select id="dbSchemaSelect">`
  - `static/css/styles.css`: Стили для селектора схемы, прокручиваемого списка, таблиц, textarea
  - `sozd_chat.py`: Удалены выполненные TODO (7 пунктов)

- **Алгоритмы**:
  - `isNumericColumn()`: Проверка на числовой тип (все значения в первых 5 строках - числа)
  - `containsUrl()`: Обнаружение URL по регулярному выражению `/^(https?:\/\/|www\.)/i`
  - `getColumnMaxLength()`: Анализ максимальной длины с учётом переносов строк и URL

### Исправлено
- Высота textarea точно соответствует содержимому (+4px для компенсации padding/border)
- Предотвращение появления скроллбара в textarea (overflow-y: hidden)
- Корректное распределение ширины между текстовыми колонками
- URL колонки получают достаточную ширину для отображения

## [2025-12-23 v1.0.4.6 17:18] - Исправление обновления счётчика сообщений при очистке чата

### Исправлено
- Кнопка Clear теперь корректно обновляет счётчик сообщений в названии чата (верхняя панель)
- Счётчик сообщений в скобках в списке чатов (левая панель) теперь обновляется при очистке
- Статистика чата скрывается, если в чате нет пользовательских сообщений

### Изменено
- `static/js/modules/actions.js`: Функция `clearMessages` теперь принимает параметры `chatTitleEl` и `renderChatList`
- `static/js/modules/actions.js`: Добавлен вызов `updateChatTitleWithStats()` и `renderChatList()` после очистки
- `static/js/app.js`: Обновлён вызов `clearMessages` с передачей всех необходимых параметров
- `static/js/modules/stats.js`: Функция `getChatStats` возвращает пустую строку, если нет пользовательских сообщений

## [2025-12-23 v1.0.4.5 15:45] - Обрезка длинных меток на оси графиков

### Изменено
- Метки на оси X графиков теперь обрезаются до 20 символов с добавлением "..."
- Константа `MAX_AXIS_LABEL_LENGTH` вынесена в `config.js` для централизованной настройки
- Улучшена читаемость графиков с длинными названиями категорий
- Применено для всех типов графиков: линейный, столбчатый, точечный

## [2025-12-23 v1.0.4.4 15:00] - Интерактивные графики с полным функционалом

### Добавлено
- **Плагины Chart.js для интерактивности**
  - chartjs-plugin-zoom: масштабирование и панорамирование графиков
  - chartjs-plugin-datalabels: подписи значений на графиках
  - Hammer.js: библиотека для поддержки жестов

- **Интерактивное управление графиками**
  - Ctrl+колёсико мыши: масштабирование графика (zoom in/out)
  - Перетаскивание мышью: панорамирование по графику
  - Кнопка "🔄 Сброс": возврат графика к исходному масштабу
  - Кнопка "📋 Копировать": копирование графика как PNG в буфер обмена

- **Улучшения визуализации**
  - Улучшенный tooltip с форматированием чисел (разделители тысяч)
  - Для круговой диаграммы: значение + процент в tooltip
  - Для точечной диаграммы: координаты X и Y
  - Легенда с автоматическим расчётом процентов для круговых диаграмм
  - Подписи значений прямо на секторах круговой диаграммы (если >5%)
  - Подписи на осях X и Y для всех типов графиков
  - Улучшенная сетка с настраиваемой прозрачностью

### Изменено
- Убраны заголовки (title) графиков - больше места для визуализации данных
- Отключен highlight.js для SQL кода (исправлена ошибка "require is not defined")
- Формат даты в version.json теперь включает время (YYYY-MM-DD HH:MM)

### Исправлено
- Исправлена ошибка загрузки highlight.js (CommonJS vs browser версия)
- Добавлена явная регистрация плагинов zoom и datalabels
- Исправлена работа панорамирования графиков

### Навигация по истории сообщений
- ArrowUp/ArrowDown работают только когда курсор в начале/конце поля ввода
- Сохранение черновика при навигации по истории
- Автоматический сброс навигации при отправке сообщения

## [2025-12-23 v1.0.4] - Автоматическая визуализация данных с построением графиков

### Добавлено
- **Автоматический анализ данных для визуализации**
  - Автоматическое определение пригодности SQL-результатов для построения графиков
  - Анализ типов колонок: числовые (80% threshold), категориальные, временные
  - Минимум 2 строки данных для построения графиков
  - Информативное сообщение о доступных типах графиков

- **Поддержка 4 типов графиков**
  - 📈 Линейный график (числовая Y + категориальная/временная X)
  - 📊 Столбчатый график (числовая Y + категориальная X)
  - 🥧 Круговая диаграмма (1 числовая + 1 категориальная, до 10 категорий)
  - 🔵 Точечная диаграмма (2+ числовых колонки)

- **Интерактивное модальное окно**
  - Кнопка "📊 График" появляется автоматически при подходящих данных
  - Dropdown-селектор для выбора типа графика
  - Динамическая перерисовка при смене типа
  - Закрытие по клику на X или вне области окна
  - Полная поддержка светлой и тёмной темы
  - Адаптивный дизайн для мобильных устройств

- **Технические детали**
  - Новый модуль `static/js/modules/chart.js` (~260 строк):
    - Класс `ChartAnalyzer` для анализа данных
    - Класс `ChartRenderer` для построения графиков
    - Определение типов колонок с 80% threshold
  - Интеграция с Chart.js 4.4.0 из CDN
  - Автоматическое уничтожение графиков при смене типа (предотвращение утечек памяти)
  - CSS стили с поддержкой двух тем и адаптивностью

### Изменено
- **Файлы**:
  - `static/js/modules/render.js`: Добавлена интеграция ChartAnalyzer и функция showChartModal
  - `templates/index.html`: Добавлено модальное окно и подключение Chart.js
  - `static/css/styles.css`: Добавлены стили для модального окна (~170 строк)
  - `version.json`: Обновлена версия до 1.0.4

### Критерии для типов графиков
- **Линейный**: ≥1 числовая колонка + (≥1 категориальная ИЛИ ≥1 временная)
- **Столбчатый**: ≥1 числовая + ≥1 категориальная
- **Круговая диаграмма**: ≥1 числовая + ≥1 категориальная (≤10 уникальных категорий)
- **Точечная диаграмма**: ≥2 числовых колонки

## [2024-12-22 v5] - Голосовой ввод с использованием Web Speech API

### Добавлено
- **Голосовой ввод сообщений**
  - Новая кнопка микрофона 🎤 рядом с кнопкой Send
  - Распознавание речи с использованием Web Speech API (встроенный в браузер)
  - Автоопределение языка (русский, английский)
  - Вставка распознанного текста в поле ввода для редактирования перед отправкой
  - Визуальные состояния кнопки:
    - 🎤 синий - готов к записи
    - 🔴 🎤 красный с пульсацией - идет запись
    - ⏳ 🎤 желтый - обработка речи
    - Красный - ошибка (автоматически сбрасывается через 3 секунды)
  - Автоматическое скрытие кнопки в браузерах без поддержки Web Speech API
  - Отключение кнопки во время генерации ответа
  - Обработка ошибок доступа к микрофону с понятными сообщениями

- **Технические детали**
  - Новый модуль `static/js/modules/voice.js` с классом `VoiceInput`
  - Интеграция в `static/js/app.js` с callbacks для обработки текста и состояний
  - CSS стили с анимацией пульсации для состояния записи
  - Cleanup при выгрузке страницы

### Поддержка браузеров
- ✅ Google Chrome (полная поддержка)
- ✅ Microsoft Edge (полная поддержка)
- ⚠️ Safari (частичная поддержка)
- ❌ Firefox (не поддерживается, кнопка скрыта)

## [2024-12-21 v4] - Критическое исправление GCS credentials через base64

### Исправлено
- **КРИТИЧЕСКОЕ: Исправлена передача GCS credentials через base64 кодирование**
  - Попытка 1: `set /p` читал только первую строку JSON
  - Попытка 2: `for /f` loop ломал многострочный JSON
  - Попытка 3: PowerShell вызов gcloud - ошибка парсинга спецсимволов
  - **РЕШЕНИЕ**: Base64 кодирование в deploy скриптах, декодирование в Python
  - Переменная окружения: `GCS_CREDENTIALS_B64`
  - Новая функция `get_gcs_credentials_json()` с поддержкой base64
  - Обратная совместимость с `GCS_CREDENTIALS` и файлом
  - Обновлены `deploy.bat` и `deploy.sh`
  - Решены все ошибки: "Expecting property name...", "Expecting value...", "Bad syntax for dict arg"

## [2024-12-21 v3] - Исправление загрузки файлов через fetch

### Исправлено
- **Критическое исправление загрузки файлов**
  - Функция `downloadFromGCS` переписана с использованием `fetch()` и `Blob` вместо прямых ссылок
  - Теперь запросы корректно отображаются в Network tab браузера
  - Исправлена проблема с тихими сбоями загрузки в Cloud Run
  - Добавлена детальная обработка ошибок с понятными сообщениями пользователю

## [2024-12-21 v2] - Исправление работы в Cloud Run

### Исправлено
- **Загрузка файлов в Cloud Run**
  - Добавлены CORS заголовки для корректной работы с браузером ARC
  - Исправлена работа endpoint `/api/download` в Cloud Run окружении
  - Файлы теперь корректно загружаются как локально, так и в Cloud Run

### Добавлено
- **Диагностический endpoint `/api/health`**
  - Проверка наличия и корректности GCS credentials
  - Тестирование доступа к GCS bucket
  - Помогает диагностировать проблемы с конфигурацией

- **Подробное логирование**
  - Логирование всех этапов загрузки файла
  - Детальные сообщения об ошибках с полными трассировками
  - Логи доступны в Cloud Run Console для отладки

### Изменено
- **Улучшена обработка ошибок**
  - Все исключения логируются с полным stack trace
  - Более информативные сообщения об ошибках

## [2024-12-21 v1] - Скачивание файлов из Google Cloud Storage

### Добавлено
- **API endpoint `/api/download`** для безопасного скачивания файлов из Google Cloud Storage
  - Поддержка автоматической распаковки .7z архивов
  - Обработка файлов, хранящихся в поддиректориях по номерам законопроектов
  - Автоматическая замена расширения файла на .7z, если оригинал не найден

- **Кнопки скачивания в UI**
  - Интерактивные кнопки для полей `filename_bucket` в табличном выводе
  - Интерактивные кнопки для полей `filename_bucket` в текстовом выводе
  - Кнопки работают с регистронезависимыми названиями колонок (FILENAME_BUCKET, filename_bucket)
  - Иконка 📥 для визуальной идентификации кнопок скачивания

- **Безопасность**
  - Файл `.env.example` с примером структуры переменных окружения
  - Файл `SECURITY.md` с инструкциями по безопасной работе с GCS credentials
  - Обновлен `.gitignore` для исключения файлов с чувствительными данными:
    - `gcs_credentials.json`
    - `.env`, `.env.local`
    - `__pycache__/`

- **Скрипты развертывания**
  - Обновлены `deploy.sh` и `deploy.bat` для передачи GCS credentials через переменные окружения
  - Автоматическая загрузка credentials из файла при деплое
  - Обновлена документация `DEPLOY.md` с требованиями к GCS credentials

### Изменено
- **Зависимости** в `requirements.txt`:
  - Добавлен `google-cloud-storage` - для работы с Google Cloud Storage
  - Добавлен `py7zr` - для распаковки .7z архивов

- **Стили** в `static/css/styles.css`:
  - Добавлены стили для кнопок скачивания `.download-btn`
  - Зеленая цветовая схема для визуального отличия от других кнопок

- **Конфигурация** в `static/js/modules/config.js`:
  - Добавлена константа `GCS_BUCKET` с именем bucket

### Технические детали
- **Backend** (`sozd_chat.py`):
  - Новый endpoint `/api/download` с поддержкой:
    - Загрузки из GCS с использованием service account
    - Fallback на файл credentials, если env var не задана
    - Автоматического поиска .7z версии файла
    - Распаковки архивов во временной директории
    - Корректной передачи MIME-типов

- **Frontend** (`static/js/modules/`):
  - `formatters.js`: Генерация HTML кнопок для filename_bucket в таблицах и тексте
  - `render.js`: Обработчики событий для кнопок скачивания
  - `utils.js`: Функция `downloadFromGCS()` для инициации скачивания
  - Кнопки работают через делегирование событий для динамического контента

### Исправлено
- Корректная обработка путей к файлам с префиксом номера закона
- Правильная замена расширения (не добавление) при поиске .7z версии
- Регистронезависимая проверка имени колонки filename_bucket

---

## [2024-12-20] - Сохранение и передача session_id

### Исправлено
- Session ID теперь сохраняется в localStorage и восстанавливается при перезагрузке страницы
- Session ID передается в request body вместо URL параметров для запросов к `/sql` и `/text` endpoints

### Изменено
- `static/js/modules/state.js`: Добавлена персистентность session_id через localStorage
- `static/js/modules/api.js`: Session ID перемещен из URL в тело запроса

---

## [2024-12-19] - Универсальные скрипты развертывания

### Добавлено
- Универсальные скрипты `deploy.sh` и `deploy.bat` с параметрами
- Документация `DEPLOY.md` по процессу развертывания

### Изменено
- Упрощен процесс развертывания с возможностью выбора региона и порта

---

## Более ранние версии

### [2024-12-18] - Улучшения UI
- Кнопки Copy и + видны при свернутом сообщении
- Улучшена индикация свернутых сообщений

### [2024-12-17] - Оптимизация и рефакторинг
- Модульная структура JavaScript кода
- Индикация количества чатов, сообщений и объема памяти
- Индикация выбранного чата
- Индикация длинных операций
